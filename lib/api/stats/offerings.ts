// Agrégation des ventes d'offrandes pour dashboard
// Cette fonction doit être adaptée à votre ORM ou driver MongoDB/PostgreSQL



/**
 * Retourne les offrandes les plus vendues et le total vendu par élément
 * @returns Array<{ offeringId: string, name: string, totalSold: number, totalRevenue: number }>
 */

export async function getMostSoldOfferings() {
  // À implémenter selon votre base de données (MongoDB, Prisma, etc.)
  // Exemple MongoDB supprimé pour éviter l'erreur de build.
  throw new Error('getMostSoldOfferings doit être implémenté selon votre base de données.');
}
