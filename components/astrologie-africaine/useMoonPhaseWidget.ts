import { useCallback, useMemo, useRef } from "react";

export function useMoonPhaseWidget({ getAdvice, getPhaseConfig, normalizeIllumination, useThrottle, useReducedMotion, MONTH_NAMES, LEGEND_ITEMS, containerVariants, dayVariants, modalVariants }: any, props: any) {
  // Place ici la logique d'état et de gestion du widget, extraite de MoonPhaseWidget
  // ...
  // Pour la démo, on retourne simplement les props
  return props;
}
