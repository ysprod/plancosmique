'use client';
import { memo } from 'react';
import AccoustiquePrimaire from '@/components/accoustique/AccoustiquePrimaire';

function AccoustiquePage() {
  return <AccoustiquePrimaire />;
}

export default memo(AccoustiquePage);
